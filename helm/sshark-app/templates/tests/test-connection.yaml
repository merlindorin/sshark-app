apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sshark.fullname" . }}-test-connection"
  labels:
    {{- include "sshark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sshark.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
