apiVersion: v1
kind: Service
metadata:
  name: {{ include "sshark.fullname" . }}
  labels:
    {{- include "sshark.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "sshark.selectorLabels" . | nindent 4 }}
